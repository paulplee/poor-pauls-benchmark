typer>=0.9.0
rich>=13.0.0
huggingface_hub>=0.20.0
pydantic>=2.0.0
python-dotenv>=1.0.0
nvidia-ml-py>=12.0.0  # optional — NVIDIA GPU detection
httpx>=0.27.0

# ---------------------------------------------------------------------------
# Optional: qualitative context-rot evaluation
#
# llama-cpp-python needs a GPU-enabled build; a plain pip install gives
# CPU-only inference.  See pyproject.toml [project.optional-dependencies]
# for the full build instructions, or install a pre-built GPU wheel:
#
#   CUDA 12.4 (Linux/Windows):
#     CMAKE_ARGS="-DGGML_CUDA=on" pip install llama-cpp-python>=0.3.0
#
#   Metal (macOS Apple Silicon):
#     CMAKE_ARGS="-DGGML_METAL=on" pip install llama-cpp-python>=0.3.0
#
# Or via uv:
#   uv pip install "poor-pauls-benchmark[qualitative]"
# ---------------------------------------------------------------------------
# llama-cpp-python>=0.3.0

# Optional: required for BFCL tool-accuracy evaluation
# datasets>=2.0.0
